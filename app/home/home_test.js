'use strict';

describe('blubeta.home module', function() {

  beforeEach(module('blubeta.home'));

  describe('about controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var homeCtrl = $controller('homeCtrl');
      expect(homeCtrl).toBeDefined();
    }));

  });
});
